# Domain docs

This is a single-context repository.

Before exploring, read the root `CONTEXT.md` and relevant decisions under `docs/adr/`. Proceed silently if either is absent.

Use the glossary's canonical vocabulary and avoid its rejected synonyms. Surface any conflict with an existing ADR instead of silently overriding it.
